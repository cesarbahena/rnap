services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: rnap
      POSTGRES_PASSWORD: rnap
      POSTGRES_DB: rnap
    ports:
      - "5432:5432"
    volumes:
      - rnap_data:/var/lib/postgresql/data

volumes:
  rnap_data: